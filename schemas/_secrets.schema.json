{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "data/_secrets.json — 密钥（不入库、不外传）",
  "type": "object",
  "properties": {
    "api_key": { "type": "string" },
    "tushare_token": { "type": "string" }
  }
}
